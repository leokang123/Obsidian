---
cssclasses:
  - wide-page
tags: 
주제: 
생성일: 2024-07-21 15:47
수정일:  2026-03-24 19:55:39
banner: "![[library.jpg]]"
banner_y: 0.43333
---

# 공부

`````ad-multi-column
````ad-blue
title: 해야할 공부
```tasks  
not done
tag includes #study
due on or after today
hide toolbar
hide tags
hide edit button
hide backlink
```
````

````ad-purple
title: Done
```tasks  
done  
done today
tag includes #study
hide toolbar
hide tags
hide due date
hide recurrence rule
hide edit button
short mode  
```
````
`````

~~~ button 
id makeFileInDataview
color green
~~~

````ad-cyan
```dataviewjs
const list = dv.pages('#일반').filter(p => p.file.folder === "Studied");
// const semesters = ["3-1","3-2","4-1","4-2"]
const filterItem1 = ["대학교", "회고"] //개인 학습과 거리가 먼거 
const filterItem2 = ["일반", "중요", "잡", "학습정리", "네이버", "개념", "프로젝트", "책리뷰"]; // 세부적인 주제에 관한것만 
const filterPage = list.filter(p => !p.tags.some(q => filterItem1.includes(q))); // 학습과 관련된거 필터링
const sortPage = filterPage.sort(page => page.수정일, "desc")
const tagSet = new Set(sortPage.tags);
const tagList = [...tagSet];
const filterList = tagList.filter((item) => !filterItem2.includes(item)) // 세부적인 학습에 관련된거 필터링
let res = '';
filterList.forEach((tag) => {
res += `~~~ dataview
TABLE WITHOUT ID
link(file.path,file.name) AS "${tag} :LiBookCheck:"
FROM "Studied" and #${tag}
WHERE !contains(file.tags, "대학교")
SORT 주제, series DESC
~~~\n<br>\n\r`;
})
dv.paragraph(res);
```
````

`````ad-multi-column
````ad-blue
```dataview
TABLE WITHOUT ID
link(file.path,file.name) AS "학습정리",
dateformat(file.mtime, "yyyy년 MM월 dd일 HH:mm") AS "" 
FROM #학습정리 AND "Studied"
SORT 주제, series ASC
```
````

````ad-purple
```dataview
TABLE WITHOUT ID
link(file.path,file.name) AS "중요",
dateformat(file.mtime, "yyyy년 MM월 dd일 HH:mm") AS "" 
FROM #중요 AND "Studied"
SORT 주제, series  ASC
```
````
`````
