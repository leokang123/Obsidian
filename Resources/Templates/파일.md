---
주제: <%* if (Array.isArray(tp.file.title.match(/\((.*)\)/)))  tR += tp.file.title.match(/\((.*)\)/)[1]; else tR +=  "Default";%>
cssclasses: wide-page
생성일: <% tp.date.now("yyyy-MM-DD HH:mm") %>
수정일: 2024년 08월 04일 오후 19시 00분
series: <% Number(tp.file.title.match(/\d+/)) + 1 || 1 %>
---

# <% tp.file.title %>
<%* if (Array.isArray(tp.file.title.match(/\((.*)\)/)))  tR += "#" + tp.file.title.match(/\((.*)\)/)[1];%>
<%* console.log(tp.file) %>